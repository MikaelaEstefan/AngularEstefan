import { EdadFormatPipe } from './edad-format.pipe';

describe('EdadFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new EdadFormatPipe();
    expect(pipe).toBeTruthy();
  });
});

